import styled from "styled-components";

import { Footer } from 'grommet';

const Container = styled(Footer)`
    width: 100%;
    color: #fff;

    position: fixed;
    bottom: 0;
    left: 0;
`
export {
    Container,
}